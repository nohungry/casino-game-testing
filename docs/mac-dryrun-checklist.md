# macOS 試跑檢查表（Mac QA 第一次上手用）

> **目的**：請一位用 macOS 的 QA，照這份從零跑一輪第三方遊戲測試，把**實際遇到的狀況**填進每個「📝 結果」欄。
> 跑完把這份回傳，會用你的**真實結果**補進 `README.md` 的「macOS 專屬」說明（目前 Mac 的步驟多是推測、未驗證）。
> 填寫方式：`[ ]` 做到了就改成 `[x]`；每個 📝 後面寫你實際看到什麼（順、卡、跟預期不同都要寫）。

---

## 0. 開始前要先跟團隊拿的東西
README 刻意不存站點/帳號（站點無預設），所以這些要**另外索取**：

- [ ] 測試站 **URL**：`__________________`
- [ ] 測試**帳號 / 密碼**（前台登入用）：`__________________`
- [ ] **後台 bet-report** 存取（帳號 + 網址，post 對帳用）：`__________________`
- [ ] 這次要測的**品牌**與 sample 遊戲：`__________________`

📝 結果（這步有沒有卡？文件有沒有講清楚去哪拿？）：
```

```

---

## 1. 裝 Claude Code 與前置
- [ ] 已安裝 **Claude Code**（怎麼裝的：`__________`，版本：`__________`）
- [ ] 已安裝 **Node.js**：`node -v` → `______`，`npx -v` → `______`
- [ ] `git clone` 專案 → `cp .mcp.json.example .mcp.json`

📝 結果（Node 是用什麼裝的？brew / nvm / 官網安裝包？有沒有 PATH 問題？）：
```

```

---

## 2. 裝 Chromium
- [ ] 跑 `npx playwright install chromium`
- [ ] **觀察點**：這步在 Mac 是**必要**的嗎？還是不裝、直接啟動 Claude Code 時 @playwright/mcp 也會自己下載？

📝 結果（有沒有下載？下載到哪？花多久？或其實不需要這步？）：
```

```

> macOS 不需要 Linux 那些系統 lib（libpango 等），也通常不用裝中文字型（系統內建）。若繁中仍有問題請記在第 6 點。

---

## 3. 啟動 Claude Code → MCP / 權限
- [ ] 在專案資料夾啟動 Claude Code
- [ ] **觀察點 A**：playwright / chrome-devtools 這兩個 MCP server **有自動啟用**嗎？還是要手動同意？（`.claude/settings.local.json` 是個人檔、不在 repo，所以新人可能沒有任何預設權限）
- [ ] **觀察點 B**：用瀏覽器工具時，是否**每個工具都跳權限確認**？大概要點幾次同意才順？

📝 結果（MCP 啟用流程實際長怎樣？權限要批准哪些？這段 README 沒寫，你的實況會補進去）：
```

```

---

## 4. Chromium 視窗 + 滿版
- [ ] **觀察點 A**：Chromium 視窗**有自動跳出**嗎？
- [ ] **觀察點 B**：跳出後**是不是滿版**？（config 用 `--start-maximized`，這 flag 在 Mac 不一定有效）
- [ ] 若沒滿版，你怎麼弄滿版的？（手動拖、按綠色鈕全螢幕、還是…）

📝 結果（`--start-maximized` 在你的 Mac 有效嗎？滿版的正確做法是什麼？）：
```

```

---

## 5. 🔴 Retina / 座標（最關鍵，務必做）
進到一款遊戲的可玩畫面後，請 AI 幫你在 console 印出來並記下：

- [ ] `window.devicePixelRatio` = `______`（Mac Retina 預期是 **2**）
- [ ] `window.innerWidth` × `innerHeight` = `______ × ______`
- [ ] 截一張全畫面圖，看**圖檔實際像素尺寸** = `______ × ______`
- [ ] **觀察點**：截圖像素是不是 viewport 的 **2 倍**？（這會影響「從截圖回推 SPIN 座標」的換算）

📝 結果（dpr、截圖與 viewport 的倍率關係）：
```

```

---

## 6. 跑一輪 calibrate → run
- [ ] `/test-game-brand calibrate <brand>`（停在一款 sample 遊戲）
- [ ] 校出的 **SPIN 座標點下去準不準**？有沒有因 Retina 點歪？
- [ ] 餘額**視覺判讀**正常嗎？繁中有沒有變方塊 □？
- [ ] `/test-game-brand run <brand> --range 1-3`（停在遊戲列表頁）→ 3 款能不能跑、餘額 delta 驗得出來？

📝 結果（座標準度、餘額讀取、跑批是否順）：
```

```

> ⚠️ 提醒：座標是**綁這台 Mac 的螢幕/解析度**的，**不能**直接抄別人（尤其 Windows）的 `brands/<brand>.yaml`，每台要自己 calibrate。

---

## 7. 跑一輪 post 對帳（可選）
- [ ] 手動開好後台 bet-report、篩好條件（帳號/品牌/時間涵蓋本次）
- [ ] `/test-game-brand post <brand>` → 對帳結果對得上嗎？

📝 結果：
```

```

---

## 8. 彙總：README 該補什麼
把上面跑下來「**文件沒講、我自己摸出來**」的所有點，條列在這（這就是要補進 README「macOS 專屬」的素材）：

1.
2.
3.

---

填完請把這份回傳。謝謝！
